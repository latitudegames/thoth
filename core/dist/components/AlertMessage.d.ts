import { ThothReteComponent } from "./ThothReteComponent";
export declare class Alert extends ThothReteComponent {
    constructor();
    builder(node: any): any;
    worker(node: any, inputs: any, data: any): Promise<void>;
}
